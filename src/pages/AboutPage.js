import React from "react";
import css from "./Pages.module.css";

const AboutPage = () => (
  <div className={css.wrapAbout}>
    <h2 className={css.titleAbout}>About us</h2>
  </div>
);

export default AboutPage;
